server:
  port: 7001

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/whalerain/repo-demo.git
          search-paths: springcloud-demo/config-server
          skip-ssl-validation: true

